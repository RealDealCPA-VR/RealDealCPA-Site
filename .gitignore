# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
*.log

# Env / secrets
.env
.env.local
.env.*.local

# Dependencies
node_modules/

# Vite cache
.vite/

# NOTE: dist/ is intentionally NOT ignored — cPanel git deploy needs the
# committed build output to rsync into public_html (see .cpanel.yml).
# Run `npm run build` before each `git push`.
